import { MergeFilterNationToQueryParamsPipe } from './merge-filter-nation-to-query-params.pipe';

describe('MergeFilterNationToQueryParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new MergeFilterNationToQueryParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
